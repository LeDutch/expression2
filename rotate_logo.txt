@name Rotate Logo
@inputs Gimbal:wirelink
@persist Angle
@trigger 
@strict

interval(30)

if (~Gimbal) {
    Gimbal["On", number] = 1 
}

if (Gimbal != nowirelink()) {
    Gimbal["Angle", angle] = ang(0,Angle,0)
}

Angle -=1
