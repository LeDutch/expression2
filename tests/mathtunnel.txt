@name Tomato Math Tunnel
@inputs 
@outputs 
@persist I Angle Size Coords
@trigger 
@strict

if (~EGP) {reset()}
if (first() || dupefinished()) {
    #include "library/tomato_lib"
    Size = 512
    Angle = 0
    Coords = 0
}

interval(100)

I+=1
EGP:egpClear()
createBox("Box"+I,size(vec2(ScreenXY,ScreenXY)), color(White), coords(vec2(0,0)))
EGP:egpAngle(I, Angle)
EGP:egpColor(I, vec(0,0,0))
Size-=6
Coords + 3
