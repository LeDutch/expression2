@name GIF Test
@inputs 
@outputs 
@persist 
@strict

#include "lib/tomato/1.1/tomato_egp"
#include "lib/tomato/1.1/tomato_ui"
#include "lib/tomato/1.1/tomato_core"
#include "lib/tomato/1.1/tomato_sprites"

if (initial()) {
    tauPrecacheMaterial("3099087")
    let Animation = 50
    timer(0.1, 0, function(){
        spriteByIndex("ClockTower", table(
            "x"=256-64, "y"=256-64, "w"=128, "h"=129,
            "index"=Animation,
            "uv_size"=vec2(128, 129),
            "sheet_size"=vec2(640, 1806),
            "material"="../data/tau/cache/files/1a2b6f5882d9172b9d6ab62bfc96921a.png"
        ))
        if (Animation == 68) {
            Animation = 1
        }
        else {
            Animation++
        }
    })
}
